# Phase 4 Build Notes

Phase 4 changes are intentionally limited to the native recovery foundation and the plugin history API. No UI redesign and no JavaScript operation implementation was added.

CI must run `gradle -p android-custom assembleDebug` (or the repository's configured Android task) to verify the Kotlin compilation in the target environment.
